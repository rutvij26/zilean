services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: zilean
      POSTGRES_USER: zilean
      POSTGRES_PASSWORD: zilean
    ports:
      - "5432:5432"
    volumes:
      - zilean_data:/var/lib/postgresql/data

volumes:
  zilean_data:
